import { DataSourcesSection } from '@/components/settings/data-sources';

/** Settings → Datasources list page. */
export default function SettingsDataSourcesPage() {
  return <DataSourcesSection />;
}
